import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bakesmart2d.app',
  appName: 'BakeSmart2D',
  webDir: 'dist',
  android: {
    backgroundColor: '#2b1a0d'
  }
};

export default config;

